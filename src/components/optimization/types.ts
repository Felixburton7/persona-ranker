/**
 * Re-export optimization types from centralized location.
 * This file exists for backward compatibility with existing imports.
 */
export type { OptimizationRun, PromptVersion } from '@/types/optimization';

